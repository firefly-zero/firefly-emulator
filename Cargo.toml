[package]
name = "firefly-emulator"
version = "0.1.0"
edition = "2021"

[dependencies]
firefly-hosted = { path = "../firefly-hosted" }
firefly-runtime = { path = "../firefly-runtime" }
embedded-graphics = "0.8.1"
embedded-graphics-simulator = "0.6.0"
